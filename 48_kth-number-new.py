def solution(array, commands):
    results = []
    for command in commands:
        i, j, k = command
         = [array[i]]
        for k in range(commands[k]):
            array
            
    return results